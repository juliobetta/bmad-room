---
idea: Multi-process agent hive + UI for BMad personas, forking Munder Difflin's architecture (file-based mailbox, GOD orchestrator, office-floor visualization) but wired to BMad personas (Analyst, PM, Architect, Dev, UX Designer, etc.) instead of generic CLIs. New separate project.
goal: Make it better: drive open questions to concrete, buildable decisions.
status: complete
updated: 2026-09-17T22:52
---

- (decision) Reject Munder Difflin's core architecture: agents as separate live CLI processes wrapped via node-pty. Keep BMad's existing logic: personas run as skills/agents within Claude Code sessions, not as independent OS processes.
- (kill) Monetization / paid-tier planning (Munder Difflin's Free/Pro/Teams model) is out of scope. User confirmed: not building a sellable product, intends FOSS release. Teams' only real moat (cross-device sealed clone-to-clone messaging) doesn't map onto a single-session/single-machine BMad UI anyway.
- (crack) Root cause of '@ shortcuts break' found in bmad-hub/docs/rfcs/bmad-hub.md line 138 (self-documented known limitation): Claude Code always resolves relative paths/@ mentions against cwd. activate_project.py launches sessions with cwd inside projects/<name>/ (symlink dir), not the real checkout at <local-path>. CLAUDE.md's working-directory pointer is a prompt-level instruction, not a structural fix -- it has to be followed each turn, so it fails intermittently. This is the structural source of the pain, not a visualization gap.
- (lock) This extends bmad-hub in place, not a new separate repo/project. UI + cwd fix live inside bmad-hub, sharing config/projects.toml, skills/, and per-project artifacts/memory/docs as the single source of truth.
- (lock) Cwd fix approach confirmed as the direction: activation launches the session with cwd = the real project checkout (<local-path>), not projects/<name>/. BMad's canonical skills/_bmad overlay into the real checkout via symlinks (ephemeral/gitignored) so artifact/memory/docs routing still targets hub-managed per-project storage. No known reason the original design avoided this -- worth prototyping.
- (decision) Symlinks placed into the real checkout (skills/_bmad overlay) are excluded via .git/info/exclude (local, untracked), not a committed .gitignore -- avoids polluting the app repo's own history/visible config for teammates not using the hub.
- (kill) GitHub sync of artifacts is not a gap -- bmad-hub is already a normal git repo with a remote (juliobetta/bmad-hub), plain git covers it. Not a requirement for this idea.
- (crack) 'Syncing skills from each project' isn't a missing feature (mirroring already exists both directions: canonical->project and project-local->project session) -- it's a TRUST/VISIBILITY gap: user isn't confident the existing sync is actually happening correctly. Same shape as the @ shortcut and memory-routing complaints: the underlying mechanisms exist but there's no way to SEE whether they're working, so failures are only discovered indirectly.
- (lock) Dashboard content confirmed: per-project sync status (canonical vs mirrored skills), cwd-correctness of last launch, memory/artifact routing sanity check, cross-project activity feed.
- (note) Prior scripts/tui/ attempt (pty_pane.py etc, source deleted) failed on UI/interaction quality -- 'terrible, very cumbersome interaction' -- not on the underlying problem being wrong. Leaning away from repeating a TUI without addressing why it was cumbersome.
- (crack) User confirmed directly: the chat-UI pivot is driven by interface feel/excitement ('I just want it to feel like I'm in a chat app with real people'), not an unmet capability gap. Dr. Quinn's JTBD question had no capability answer.
- (lock) Chat UI approach: a web front-end that renders real BMad-in-Claude-Code execution as a chat room (persona bubbles, per-project rooms, party-mode as group chat), not a from-scratch agent runtime. Exact mechanism for bidirectional real-time interaction (pty-wrapping a Claude Code process vs driving a session via the Claude Agent SDK's structured message stream) is an open technical question for the architecture/planning phase, not resolved here.
- (note) User asked to reset framing: design the chat UI idea fresh, not anchored to bmad-hub's current file layout/activation mechanics. The underlying problems found via bmad-hub (cwd/relative-path resolution, no sync/routing visibility) still hold conceptually and don't disappear just because we stop referencing bmad-hub's specific implementation -- but the earlier 'extend bmad-hub in place' lock is now open for reconsideration, not necessarily reversed.
- (lock) Correct per-project context resolution + trustworthy memory/artifact/history routing are the internal foundation, not a separate user-facing product or screen. The chat UI is the ONLY surface the user interacts with; it can show sync/routing state inline (system messages) rather than a dedicated dashboard.
- (lock) History scope: one room per project, not one cross-project thread. Matches existing per-project memory/context scoping.
- (lock) Persona mapping: each BMad persona (Mary, John, Winston, Amelia, Sally, etc.) is its own persistent contact/DM per project, not one assistant switching voices in a shared thread. Party mode = a group chat room with multiple persona contacts present.
- (note) Open technical question deferred to architecture/planning phase, not decided in this forge: how bidirectional real-time chat is actually driven -- pty-wrapping a real Claude Code process vs. driving sessions via the Claude Agent SDK's structured message stream. Also deferred: final repo home (bmad-hub successor vs fresh repo).
