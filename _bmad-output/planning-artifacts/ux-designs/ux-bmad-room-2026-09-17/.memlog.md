---
topic: BMad Chat UI — UX
updated: 2026-09-17T23:56
---

- (decision) form factor: local web app (not desktop/mobile), confirmed by user
- (decision) stakes: hobby project
- (decision) primary inspiration/reference: Slack — user is used to working in it as a chat app
- (decision) IA mapping confirmed: project switcher rail (like Slack's workspace switcher) on the far left, one icon per project; within a project, BMad personas listed as DMs with presence; party mode presented as a group channel
- (decision) theme: both light and dark supported, user-toggleable, no strong default lean
- (note) working mode: coaching path, talk through IA/screens/flows before visuals
- (decision) form factor confirmed: local web app (browser UI + local backend daemon), not a native desktop app. The local backend already needs to pty-wrap the real Claude Code CLI process per the spec, so it can also shell out to open a real terminal/Claude Code session when the user needs to drop into it directly
- (decision) subagent/background-agent visibility: nested, collapsible tool-call-style cards inline inside the persona's message thread, mirroring how Claude Code displays its own Task tool calls and results
- (decision) party mode confirmed: user creates a channel and describes the task in natural language; personas are auto-selected based on that description, same pattern as Claude Code's own subagent auto-selection — no manual persona picker required
- (note) journey: user opens app, clicks a persona (e.g. Amelia), works a specific issue; persona greets like in real Claude Code, works autonomously, asks questions when needed; user needs a stop/interrupt control on the running turn; user needs an escape hatch to open the real Claude Code/terminal session directly
- (decision) add project flow: 'add project' action opens a native local folder picker; app validates it's a real checkout and creates the room/icon in the project switcher rail
- (decision) notifications: browser notifications fire when a persona finishes a turn or needs input in a room the user isn't currently viewing (tab unfocused or a different room open)
- (note) no further catch-all items surfaced; content density, shortcuts, message formatting details deferred — user expects to keep iterating on these rather than deciding upfront
- (event) rendered color-theme sketch: .working/color-themes-1.html, 5 variations, light+dark coverage
- (decision) color theme direction chosen: Terminal Green — developer-terminal coded, phosphor-CRT heritage, leans into the real Claude Code CLI underneath. Selected from 5 rendered variations in .working/color-themes-1.html
- (event) DESIGN.md and EXPERIENCE.md drafted from memlog decisions + SPEC.md + chosen Terminal Green palette; both currently status: draft pending reviewer gate and open-item triage
- (assumption) theme default: follows OS/browser prefers-color-scheme on first load, remembers manual toggle after — inferred since user said 'both, no strong default lean' without specifying an initial-load rule
- (assumption) baseline composer behavior assumed: Enter sends, Shift+Enter newline — standard chat convention, not explicitly requested
- (note) non-blocking open item: content density, additional keyboard shortcuts, and message-formatting details (code blocks/diffs/markdown) explicitly deferred by user for later iteration — flagged in EXPERIENCE.md rather than invented
- (decision) reviewer gate skipped at user's request — hobby project, expects to keep iterating rather than run a formal validation pass now
- (event) rendered key-screen mocks: key-app-shell-dm-thread.html (light+dark), key-party-mode-channel.html
- (event) promoted key-screen mocks .working/key-app-shell-dm-thread.html and .working/key-party-mode-channel.html to mockups/app-shell-dm-thread.html and mockups/party-mode-channel.html; EXPERIENCE.md composition references and inline flow links updated to point at them
- (decision) mock coverage confirmed: Add project and Settings stay spine-only, no dedicated mock needed — user approved the two rendered mocks without requesting more
- (event) doc_standards applied (bmad-review lenses=structure,prose): 1 minor prose fix applied (Subagent card row pronoun clarity), structure preserved as-is, 0% reduction. EXPERIENCE.md status set to final; DESIGN.md left without a status field per the Google Labs spec's fixed frontmatter token set (name/description/colors/typography/rounded/spacing/components only)
- (event) spines finalized
