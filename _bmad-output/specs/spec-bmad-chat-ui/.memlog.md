---
topic: BMad chat UI
updated: 2026-09-18T14:58
---

- (capability) CAP-1: chat UI renders real BMad-in-Claude-Code execution as a chat room; not a separate agent runtime — a skin over real Read/Write/Edit/Bash/skills execution
- (capability) CAP-2: one room per project with correct per-project cwd/context resolution as invisible plumbing; sync/routing state surfaces as inline system messages, not a dashboard
- (capability) CAP-3: each BMad persona is a persistent, per-project contact (DM), not one assistant switching voices
- (capability) CAP-4: party mode presented as a group chat room
- (constraint) chat skin only — real agent execution stays on Read/Write/Edit/Bash/skills, never reimplemented on a separate stack
- (constraint) fix direction is fixed: launch sessions with cwd = the real project checkout; overlay BMad's canonical skills/_bmad via symlinks excluded through .git/info/exclude, not a committed .gitignore — this is the root fix for the @ mention / misrouted artifact bug
- (constraint) FOSS, not a sellable product — no paid-tier planning enters design decisions
- (constraint) one room per project, never a single cross-company thread
- (note) non-goal: Munder Difflin-style architecture (per-agent OS processes via node-pty, file-based mailbox, GOD orchestrator, office-floor visualization) — rejected, personas stay running as skills inside real sessions
- (note) non-goal: monetization / paid tiers — confirmed not building a sellable product
- (note) non-goal: a standalone dashboard UI as its own surface — killed once the chat UI absorbed that job
- (note) non-goal: GitHub sync of artifacts as a requirement — not a gap, plain git already covers it
- (note) non-goal: a full custom agent runtime on the Agent SDK independent of Claude Code — considered, not chosen; the excitement was about interface feel, not an unmet capability, so the cheaper skin-over-real-execution path was picked
- (question) success signal is not stated in the input — needs a concrete, testable/demonstrable criterion before downstream skills can validate against it
- (question) real-time chat transport undecided: pty-wrapping a real Claude Code process vs. driving sessions via the Claude Agent SDK's structured message stream
- (question) final home undecided: extend bmad-hub in place (shares project registry, canonical skills, per-project artifacts/memory/docs) vs. a fresh repo — leaning bmad-hub, not locked
- (decision) user chose express mode for gap handling and 'bmad-chat-ui' as the spec slug
- (event) self-validate pass 1 (coherence): PASS — 4 capabilities each have intent+success, all 4 constraints rule something out, 5 non-goals present, capability IDs stable (first creation), prose lean; success signal has no concrete criterion in the input and is correctly routed to open_questions rather than invented
- (event) self-validate pass 2 (preservation): PASS — every load-bearing claim in forged-idea.md (core decisions, grounding problem + fix direction, all 5 rejected/killed items, both open questions) landed in SPEC.md; no wrapper-only drops; no companion needed, content fits the five-field kernel
- (event) SPEC.md rendered and presented to user: 4 capabilities, 0 companions, 3 open questions
- (decision) success signal resolved by user: being able to chat with a BMad agent end to end — a user opens a room, sends a message, and gets a response grounded in real Claude Code execution for that project
- (decision) real-time transport resolved: pty-wrapping the real Claude Code CLI process, not the Agent SDK's structured message stream. Reason: the Agent SDK means rebuilding the execution loop independent of Claude Code, which risks drifting into the already-rejected 'full custom agent runtime independent of Claude Code' non-goal; pty-wrapping the actual CLI binary guarantees identical skills/hooks/config/tool behavior and most literally satisfies the 'chat skin, not new runtime' constraint. User asked for a recommendation and did not push back — logged as the working decision, reversible if it proves wrong in practice
- (decision) final home resolved by user: forget bmad-hub, build this project here in bmad-room as its own thing, not as an extension of bmad-hub
- (event) spec re-derived after resolving all 3 open questions: success signal now concrete, transport constraint added (pty-wrapping Claude Code CLI), bmad-hub-extension added as non-goal, home confirmed as bmad-room. Open Questions section now empty and omitted. Spec Law rule 5 (testable success signal) now satisfied
- (note) non-goal: BMad installation/bootstrapping into a project that doesn't already have _bmad — deferred, out of scope for this pass (surfaced during epic/story breakdown)
- (note) non-goal: BMad self-update mechanics beyond the persona-catalog-drift handling already in the spec — deferred, out of scope for this pass (surfaced during epic/story breakdown)
- (note) non-goal: supporting chat harnesses/CLIs other than the real Claude Code CLI (e.g. Antigravity, OpenCode) — explicit non-goal, not just deferred; contradicts the transport constraint (pty-wrapping the real Claude Code CLI process) and would require reopening that decision, not adding a story
- (event) self-validate pass 1 (coherence): PASS — non-goals count now 9, rule 4 still satisfied; new entries are single-line, decision-bearing, lean prose (rule 8); no other kernel field affected
- (event) self-validate pass 2 (preservation): PASS — all 3 non-goal claims from this update (BMad install deferred, BMad self-update deferred, multi-harness support explicit non-goal) landed verbatim in SPEC.md Non-goals; no source document involved, direct decision append
- (event) SPEC.md re-derived with 3 new non-goals from epic/story breakdown scoping discussion
